apiVersion: v1
kind: Service
metadata:
  name: {{ include "consent-manager.fullname" . }}
  labels:
    {{- include "consent-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "consent-manager.selectorLabels" . | nindent 4 }}
