apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consent-manager.fullname" . }}-test-connection"
  labels:
{{ include "consent-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "consent-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
