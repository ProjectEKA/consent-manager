package in.projecteka.consentmanager.userauth;

import static in.projecteka.consentmanager.Constants.CURRENT_VERSION;

public class Constants {
    public static final String PATH_USER_AUTH_INIT = CURRENT_VERSION + "/users/auth/init";
    public static final String PATH_USER_AUTH_CONFIRM = CURRENT_VERSION + "/users/auth/confirm";
    public static final String PATH_USER_FETCH_AUTH_MODES = CURRENT_VERSION + "/users/auth/fetch-modes";
    public static final String PATH_ON_USER_FETCH_AUTH_MODES = CURRENT_VERSION + "/users/auth/on-fetch-modes";
}
