server:
  port: 8000
hdaf:
  clientregistry:
    XAuthToken: ${Auth_Token}
    clientId: 6
    url: ${Client_Registry_Url}
  userservice:
    url: ${User_Service_Url}
  db:
    host: localhost
    port: 5432
    schema: consent_manager
    user: postgres
    password: postgres
    poolSize: 5
